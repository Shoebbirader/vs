import { useEffect, useState } from "react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { WorkspaceState as State } from "@/components/workspaces/WorkspaceState";
import { Plug, CheckCircle, AlertCircle, RefreshCw } from "lucide-react";

export function OrganizationSettingsWorkspace() {
  const utils = trpc.useUtils();
  const settings = trpc.organizationSettings.get.useQuery(undefined, { retry: false });
  const [form, setForm] = useState({ timezone: "Asia/Kolkata", odometerMaxDailyKm: "1000", laborRatePerHour: "0", safetyContactName: "", safetyContactPhone: "" });
  const [activeTab, setActiveTab] = useState<"general" | "integrations">("general");
  const [integrationStates, setIntegrationStates] = useState<Record<string, { connected: boolean; lastSync?: string; error?: string }>>({
    telematics: { connected: false },
    government: { connected: false },
    insurance: { connected: false },
    tax: { connected: false },
    fuel: { connected: false },
    bank: { connected: false },
  });
  
  useEffect(() => { if (settings.data) setForm({ timezone: settings.data.timezone ?? "Asia/Kolkata", odometerMaxDailyKm: String(settings.data.odometerMaxDailyKm ?? 1000), laborRatePerHour: String(settings.data.laborRatePerHour ?? 0), safetyContactName: settings.data.safetyContactName ?? "", safetyContactPhone: settings.data.safetyContactPhone ?? "" }); }, [settings.data]);
  const update = trpc.organizationSettings.update.useMutation({ onSuccess: () => { toast.success("Organization settings saved"); void utils.organizationSettings.get.invalidate(); }, onError: (error) => toast.error("Settings update failed", { description: error.message }) });
  
  const handleIntegrationConnect = (provider: string) => {
    toast.success(`${provider} integration initiated. Redirecting to auth...`);
    // Placeholder for actual auth flow
  };

  const handleIntegrationSync = (provider: string) => {
    toast.loading(`Syncing ${provider}...`);
    // Placeholder for actual sync
    setTimeout(() => {
      setIntegrationStates(prev => ({
        ...prev,
        [provider]: { ...prev[provider], lastSync: new Date().toLocaleString("en-IN") }
      }));
      toast.success(`${provider} synced successfully`);
    }, 1000);
  };
  return <section className="panel workspace-form"><div><div className="panel-kicker">Superadmin governance</div><h2>Organization settings</h2><p>Configure operating controls and the internal INR labor rate used when approved work orders are posted to the ledger.</p></div><div className="settings-tabs"><button className={activeTab === "general" ? "active" : ""} onClick={() => setActiveTab("general")}>General</button><button className={activeTab === "integrations" ? "active" : ""} onClick={() => setActiveTab("integrations")}>Integrations</button></div><State loading={settings.isLoading} error={settings.isError}>{activeTab === "general" ? (<form className="invite-form" onSubmit={(event) => { event.preventDefault(); update.mutate({ timezone: form.timezone, odometerMaxDailyKm: Number(form.odometerMaxDailyKm), laborRatePerHour: Number(form.laborRatePerHour), safetyContactName: form.safetyContactName || undefined, safetyContactPhone: form.safetyContactPhone || undefined }); }}><label>Operating timezone<input required value={form.timezone} onChange={(event) => setForm({ ...form, timezone: event.target.value })} placeholder="Asia/Kolkata" /></label><label>Maximum odometer increase per day (km)<input required type="number" min="100" max="5000" value={form.odometerMaxDailyKm} onChange={(event) => setForm({ ...form, odometerMaxDailyKm: event.target.value })} /></label><label>Internal labor rate (₹ / hour)<input required type="number" min="0" step="0.01" value={form.laborRatePerHour} onChange={(event) => setForm({ ...form, laborRatePerHour: event.target.value })} /><small>Set to 0 to leave labor cost attribution disabled.</small></label><label>Safety escalation contact<input value={form.safetyContactName} onChange={(event) => setForm({ ...form, safetyContactName: event.target.value })} placeholder="Operations control room" /></label><label>Safety contact phone<input value={form.safetyContactPhone} onChange={(event) => setForm({ ...form, safetyContactPhone: event.target.value })} placeholder="+91 …" /></label><button className="primary-button" disabled={update.isPending}>{update.isPending ? "Saving…" : "Save organization settings"}</button></form>) : (<div className="integrations-grid"><article className="integration-card"><header><Plug size={20} /><h3>Telematics</h3></header><p>GPS tracking, fuel consumption, and odometer data</p><div className="integration-status">{integrationStates.telematics.connected ? (<><CheckCircle size={16} /><span>Connected</span></></> ) : (<>Not connected</>)}</div>{integrationStates.telematics.lastSync && <small>Last sync: {integrationStates.telematics.lastSync}</small>}<div className="integration-actions"><button onClick={() => handleIntegrationConnect("telematics")} className="secondary-button">Connect</button><button onClick={() => handleIntegrationSync("telematics")} className="secondary-button" disabled={!integrationStates.telematics.connected}><RefreshCw size={14} />Sync</button></div></article><article className="integration-card"><header><Plug size={20} /><h3>Government</h3></header><p>Vehicle RC verification and permit validation</p><div className="integration-status">{integrationStates.government.connected ? (<><CheckCircle size={16} /><span>Connected</span></></> ) : (<>Not connected</>)}</div>{integrationStates.government.lastSync && <small>Last sync: {integrationStates.government.lastSync}</small>}<div className="integration-actions"><button onClick={() => handleIntegrationConnect("government")} className="secondary-button">Connect</button><button onClick={() => handleIntegrationSync("government")} className="secondary-button" disabled={!integrationStates.government.connected}><RefreshCw size={14} />Sync</button></div></article><article className="integration-card"><header><Plug size={20} /><h3>Insurance</h3></header><p>Policy dates and coverage verification</p><div className="integration-status">{integrationStates.insurance.connected ? (<><CheckCircle size={16} /><span>Connected</span></></> ) : (<>Not connected</>)}</div>{integrationStates.insurance.lastSync && <small>Last sync: {integrationStates.insurance.lastSync}</small>}<div className="integration-actions"><button onClick={() => handleIntegrationConnect("insurance")} className="secondary-button">Connect</button><button onClick={() => handleIntegrationSync("insurance")} className="secondary-button" disabled={!integrationStates.insurance.connected}><RefreshCw size={14} />Sync</button></div></article><article className="integration-card"><header><Plug size={20} /><h3>Tax & GST</h3></header><p>GST return generation and filing</p><div className="integration-status">{integrationStates.tax.connected ? (<><CheckCircle size={16} /><span>Connected</span></></> ) : (<>Not connected</>)}</div>{integrationStates.tax.lastSync && <small>Last sync: {integrationStates.tax.lastSync}</small>}<div className="integration-actions"><button onClick={() => handleIntegrationConnect("tax")} className="secondary-button">Connect</button><button onClick={() => handleIntegrationSync("tax")} className="secondary-button" disabled={!integrationStates.tax.connected}><RefreshCw size={14} />Sync</button></div></article><article className="integration-card"><header><Plug size={20} /><h3>Fuel</h3></header><p>Fuel vendor API for auto-reconciliation</p><div className="integration-status">{integrationStates.fuel.connected ? (<><CheckCircle size={16} /><span>Connected</span></></> ) : (<>Not connected</>)}</div>{integrationStates.fuel.lastSync && <small>Last sync: {integrationStates.fuel.lastSync}</small>}<div className="integration-actions"><button onClick={() => handleIntegrationConnect("fuel")} className="secondary-button">Connect</button><button onClick={() => handleIntegrationSync("fuel")} className="secondary-button" disabled={!integrationStates.fuel.connected}><RefreshCw size={14} />Sync</button></div></article><article className="integration-card"><header><Plug size={20} /><h3>Bank</h3></header><p>Payment reconciliation and transaction matching</p><div className="integration-status">{integrationStates.bank.connected ? (<><CheckCircle size={16} /><span>Connected</span></></> ) : (<>Not connected</>)}</div>{integrationStates.bank.lastSync && <small>Last sync: {integrationStates.bank.lastSync}</small>}<div className="integration-actions"><button onClick={() => handleIntegrationConnect("bank")} className="secondary-button">Connect</button><button onClick={() => handleIntegrationSync("bank")} className="secondary-button" disabled={!integrationStates.bank.connected}><RefreshCw size={14} />Sync</button></div></article></div>)}</State></section>;
}

